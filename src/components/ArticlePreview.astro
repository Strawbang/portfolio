---
import type { Article } from '../types/article';

export interface Props {
  article: Article;
}

const { article } = Astro.props;

const DEFAULT_IMAGES: Record<string, string> = {
  'Dev.to': '/assets/devto-logo.png',
  'Medium': '/assets/medium-logo.png',
  'Blog': '/assets/blog-logo.png',
  fallback: '/assets/placeholder-article.jpg'
};

const imageUrl = article.imageUrl || DEFAULT_IMAGES[article.platform] || DEFAULT_IMAGES.fallback;

---

<article class="card">
  <a href={article.url} target={article.platform === 'Blog' ? '_self' : '_blank'} rel={article.platform === 'Blog' ? '' : 'noopener noreferrer'} class="card-link">
    <div class="image-container">
      <img 
        src={imageUrl} 
        alt={article.imageAlt || article.title}
        loading="lazy"
        decoding="async"
        width="600"
        height="338"
      />
      <div class="platform-badges">
        {(article.platform === 'Blog'
          ? ['Blog']
          : (article.platforms ?? [article.platform])
        ).map(p => (
          <span class="platform-badge">{p}</span>
        ))}
      </div>
    </div>
    <div class="card-body">
      <h3 class="title">{article.title}</h3>
      <p class="description">{article.description}</p>
      <footer>
        <time datetime={article.publishDate.toISOString()}>
          {article.publishDate.toLocaleDateString()}
        </time>
      </footer>
    </div>
  </a>
</article>

<style>
  .card {
    height: 100%;
    border-radius: 0.5rem;
    background-color: var(--gray-999);
    border: 1px solid var(--gray-800);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    overflow: hidden;
  }

  .card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-md);
  }

  .card-link {
    display: flex;
    flex-direction: column;
    height: 100%;
    color: inherit;
    text-decoration: none;
  }

  .image-container {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    border-radius: 0.5rem 0.5rem 0 0;
  }

  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .card:hover .image-container img {
    transform: scale(1.03);
  }

  .card-body {
    display: flex;
    flex-direction: column;
    flex: 1;
    padding: 1rem 1.1rem 1rem;
  }

  .platform-badges {
    position: absolute;
    bottom: 0;
    right: 0;
    display: flex;
    gap: 0.15rem;
  }

  .platform-badge {
    background-color: var(--accent-regular);
    color: var(--accent-text-over);
    padding: 0.2rem 0.5rem;
    font-size: var(--text-xs);
  }

  .platform-badge:first-child {
    border-top-left-radius: 0.3rem;
  }

  .platform-badges:has(.platform-badge:nth-child(2)) .platform-badge:last-child {
    background-color: var(--accent-dark);
  }

  .title {
    font-size: var(--text-lg);
    color: var(--gray-200);
    margin-top: 0;
    transition: color 0.2s ease;
  }

  .card:hover .title {
    color: var(--accent-regular);
  }

  .description {
    margin-top: 0.5rem;
    color: var(--gray-300);
    flex-grow: 1;
    font-size: var(--text-sm);
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  footer {
    margin-top: 1rem;
    display: flex;
    justify-content: flex-end;
    color: var(--gray-400);
    font-size: var(--text-sm);
  }
</style>